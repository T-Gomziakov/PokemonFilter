function ColumnEnabler() {
  // const tableContext = useContext(TableContext);

  return (
    <div>
      <h1 className="font-bold">Enable/disable columns</h1>
      {/* {tableContext} */}
    </div>
  );
}

export default ColumnEnabler;
